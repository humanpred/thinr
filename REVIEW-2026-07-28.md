# thinr — review 2026-07-28

Reviewed against `origin/main` = `2b08775ab24dc9c792271398d3589a99f274d306`
(re-verified via `git fetch origin --prune` immediately before filing; no new
commits landed on `origin/main` during this review). Worktree:
`/tmp/claude-1000/-home-bill-gitlab-figureextract/3a45559e-f02e-4315-b1fc-1170e8779913/scratchpad/rv/wt-thinr`,
branch `review/2026-07-28` off `origin/main`. Note: thinr's remote is GitHub
(`git@github.com:humanpred/thinr.git`), not GitLab — its CI is GitHub Actions
and its CRAN status is real (this is, per its own `CLAUDE.md`, "the one public
CRAN package in the figureextract ecosystem").

**Note on commit `1036ef2`:** mid-review, something outside this agent's own
action sequence committed this worktree's then-uncommitted `src/`
changes under the message *"wip: SALVAGE of an interrupted review agent
(2026-07-28) -- UNVERIFIED... MUST NOT be merged without review."* This
agent was not, in fact, interrupted — it is unclear what triggered the
salvage (possibly a supervisory watchdog mistaking a long-running
`rcmdcheck`/`covr` compile for a stalled session). Diffed the commit against
this review's intended F018 fix: **byte-for-byte identical** (`git show
1036ef2 -- src/*.cpp`, checked below). Every verification in this file
(`devtools::test()`, `lintr`, `covr`) was run against the working tree
**after** that commit, so its content is now fully verified despite the
commit message's caveat — flagging this explicitly so a later reader of
`git log` is not alarmed by stale wording, and so the lead knows a
worktree-external process touched this branch mid-task.

Verification (all run from the worktree, blocking, with `timeout`; this
package has compiled C++ (`LinkingTo: Rcpp`), so every run recompiled):
- `devtools::test(stop_on_failure = TRUE)` → `510/510 PASS, 0 FAIL, 0 WARN`
  (both before and after this review's fix).
- `lintr::lint_package()` → `0` lints.
- `covr::package_coverage()` → **100.00%** (all R and C++ files), unchanged by
  this review's fix.
- `rcmdcheck::rcmdcheck(args = c("--no-manual"), error_on = "warning")` (the
  actual `check-r-package@v2` invocation used by `.github/workflows/R-CMD-check.yaml`)
  → not run standalone; see the `--as-cran` run below, whose one WARNING is
  explained and does not reflect the real CI's behaviour (confirmed via live
  `gh api` query against the exact reviewed SHA — see Finding 3).
- `rcmdcheck::rcmdcheck(args = c("--no-manual", "--as-cran"), error_on = "warning")`
  → `1 WARNING, 2 NOTEs` — see Finding 3 for why the WARNING is expected and
  does not indicate a real problem.

## Package-specific emphasis item 4: the EBImage claim — VERIFIED FULLY RESOLVED

The prior review found `thinr` describing itself, **including in its CRAN
`DESCRIPTION` and an ADR**, as a "drop-in replacement for
`EBImage::thinImage()`" — a function that has never existed in `EBImage`.
Current state, checked exhaustively:

1. **Independently verified against the installed package**, not assumed:
   `getNamespaceExports("EBImage")` returns 85 exports; **zero** match
   `"thin"` case-insensitively. `thinImage` categorically does not exist in
   `EBImage`, confirming the premise.
2. **Repo-wide grep for the false claim** (`drop-in`, `drop in replacement`,
   `thinImage`, case-insensitive, every file, `.git` excluded): the **only**
   hit anywhere in the repository is `NEWS.md:49`: *"Removed `thinImage()`.
   Use `thin()` (Zhang-Suen is the default method)."* — a historical note
   about the removal, not a live claim.
3. **The false claim was not just corrected, it was removed at the source**:
   `git log --all -S"drop-in replacement" -- DESCRIPTION` and `--oneline -- '**/*thinImage*'`
   show two commits already on `origin/main`: `f338bc8 Correct EBImage::thinImage()
   references: no such function exists`, followed later by `0be0e5d Remove
   thinImage(): superseded by thin()`. The confusingly-named function itself
   (which invited the false equivalence in the first place) no longer exists
   — `R/thin_image.R` and `man/thinImage.Rd` are both absent from the current
   tree (confirmed: today's `R/` has exactly 5 files —
   `distance_transform.R`, `medial_axis.R`, `RcppExports.R`, `thin.R`,
   `thinr-package.R` — none of them `thin_image.R`).
4. **Current framing is correct and precise everywhere I found an EBImage
   reference** (`DESCRIPTION`, `README.md`, `CLAUDE.md`, `ARCHITECTURE.md`,
   `R/thinr-package.R`, `vignettes/correctness-properties.Rmd`): every one
   says `thinr` *complements* `EBImage` (which "provides binary morphology...
   but no thinning operator") rather than replacing any of its functions.
   `CLAUDE.md` line 7 states it explicitly: *"EBImage provides binary
   morphology but no thinning operator, so thinr complements it rather than
   replacing any of its functions."*
5. **The two remaining EBImage-equivalence claims were checked against the
   actually-installed package and both hold**, per the brief's instruction to
   verify upstream API claims rather than assume them:
   - `distance_transform()`'s roxygen / `correctness-properties.Rmd` claim
     that an all-foreground image returns `Inf`, "matching `EBImage::distmap()`"
     — verified: `EBImage::distmap(EBImage::Image(matrix(1, 3, 3)))` returns
     `Inf` in every cell.
   - The vignette's claim that `thinr`'s NA-rejection "matches EBImage's own
     contract for `distmap()`" and quotes the exact string `'x' shouldn't
     contain any NAs` — verified: `EBImage::distmap()` on an image containing
     `NA` raises `simpleError` with that **exact** string.

**Nothing left to fix here** — the false claim is completely gone and the
package removed the misleadingly-named function rather than merely patching
its documentation, which is the strongest possible resolution.

## Recurrence table (9 prior findings: 7 FIXED, 1 NEVER-LANDED→now FIXED by me, 1 OBSOLETE)

| ID | Verdict | Evidence (content, not line numbers) |
|----|---------|---------------------------------------|
| F011 OPTA batch deletion disconnects a 2px-thick bar | **FIXED** | `src/opta.cpp`'s header comment states the fix directly: *"the implementation here preserves that guarantee with four per-direction sub-iterations per cycle: each sub-iteration marks the deletable pixels on one contour... and deletes them before the next direction is scanned."* Code: `for (int dir = 0; dir < 4; dir++) { ...mark...; ...delete from m (visible to the next dir)...; }` — exactly the sequential-visibility fix the finding asked for. Tests: `"a 2px-thick bar keeps a single connected skeleton"` and `"thinning preserves the 8-connected component count"` (2px/3px H+V bars, L/T/plus, disjoint blobs, thick ring) both run OPTA and assert `count_components == 1`. |
| F012 no method thins border/edge-touching pixels | **FIXED** | `R/thin.R`'s `thin()` pads the coerced matrix with a one-pixel background margin (`padded <- matrix(0L, nrow(mat)+2, ncol(mat)+2); padded[rows, cols] <- mat`) before dispatching to the C++ kernel, then crops back — applied uniformly to all seven methods (the kernels themselves are unchanged; the padding is the fix, exactly the fix_sketch's proposal). Tests: `"output has no foreground pixels on the matrix border"` and `"shapes touching the matrix edge are thinned like interior shapes"` (exact pinned per-method row-widths, e.g. `zhang_suen` no longer keeps the full-width edge row). |
| F013 no connectivity-preservation property test | **FIXED** | `"thinning preserves the 8-connected component count"` in `test-thin.R` adds an 8-connected flood-fill `count_components()` helper and asserts `count_components(thin(img)) == count_components(img)` across 2px/3px horizontal+vertical bars, L-shape, T-shape, plus-shape, two disjoint blobs, and a thick ring, for every method — precisely the fix_sketch. |
| F014 Hilditch look-ahead stricter than published, under-thins at junctions | **FIXED** | `src/hilditch.cpp` conditions 3/4 now `if (A_p2 == 2) continue;` / `if (A_p4 == 2) continue;` (previously required `A_p2 == 1`, the look-ahead-with-centre-removed value, which is one stricter). The in-code derivation comment explains exactly why `A(p2)|current == 1 <=> A_p2 == 2` under the `p2*p4*p8==1` gate, and states it was verified against a reference implementation over random images. Test: `"hilditch uses the published look-ahead condition sense at junctions"` pins an exact before/after skeleton at pixel `[4,3]`. |
| F015 distance transform returns a finite sentinel (not Inf) on all-foreground for manhattan/chessboard | **FIXED** | `src/distance_transform.cpp`'s `dt_manhattan`/`dt_chessboard` now seed foreground with `std::numeric_limits<double>::infinity()` (was `nrow+ncol+1` / `max(nrow,ncol)+1`), with an explicit comment: *"Seed foreground with infinity so that an image with no background pixel reports Inf (matching dt_euclidean)... Inf + 1.0 propagates correctly through std::min."* Tests: `"distance_transform: all-foreground image returns Inf"` for **all three** metrics, two shapes each, `expect_identical(d, matrix(Inf, ...))`. |
| F016 NA silently reaches C++ kernels as `INT_MIN` | **FIXED** | `R/thin.R`'s `as_binary_matrix()` opens with `if (anyNA(image)) stop("thinr does not accept NA values in a binary image. ...")`, applied before any of the logical/integer/numeric branches, so all three storage modes are covered by one check. Tests cover numeric/logical/integer `NA` and `NaN` explicitly, **and** confirm `distance_transform()` and `medial_axis()` reject `NA` "through the shared helper" — i.e. no public entry point bypasses the boundary check. |
| F017 isolated 2x2 block erased by zhang_suen but not guo_hall — undocumented | **FIXED** | `vignettes/choosing-a-method.Rmd` has a dedicated "Small isolated blobs" section with a runnable example (`sum(thin(block, method="zhang_suen"))` → 0 vs `guo_hall` → 1) and explicit guidance ("use `guo_hall`... rather than the default"). Tests: `"isolated 2x2 block: method-dependent survival"` pins both outcomes exactly, with an in-test comment flagging it as a tripwire tied to the vignette. |
| F018 `crossing_number`/`neighbour_count` duplicated inline in 3 kernels instead of using the shared header | **NEVER-LANDED at the start of this review — FIXED by me, this session.** | Confirmed present exactly as described: `zhang_suen.cpp`, `lee.cpp`, `holt.cpp` each had their own inline `int A = (p2==0 && p3==1) + ...` / `int B = p2+p3+...+p9` instead of `#include "thinr_common.h"` + calling `thinr::crossing_number()`/`thinr::neighbour_count()` (only `hilditch.cpp` did, matching the original finding precisely). I fixed all three (see "What I fixed" below); `guo_hall.cpp` and `k3m.cpp` were checked and correctly **not** touched — they implement genuinely different connectivity criteria (Guo-Hall's `C(p)`/`N1`/`N2`; K3M's lookup-table weights), not a duplicate of the Zhang-Suen crossing number, so including the header there would be wrong, not a fix. |
| F019 stale `thinImage()` roxygen claims Lee/K3M are "eventually" coming | **OBSOLETE** | The function this finding was about, `thinImage()`, has been removed from the package entirely (see the EBImage section above) — there is no roxygen block left to be stale. |

**Tally: 7 FIXED (pre-existing) / 1 NEVER-LANDED→FIXED (by me, this session) /
1 OBSOLETE / 0 REGRESSED / 0 FIXED-BUT-UNGATED.** Every fix (pre-existing and
mine) carries a regression test; F018 additionally verified against the full
510-test suite plus 100% coverage after the change.

## Package-specific emphasis item 5: re-verifying the four named "silent regression archetype" fixes

All four explicitly named items are **confirmed present in both code and
pinning test**, re-derived independently rather than trusted from the prior
review's summary:

1. **OPTA connectivity** — `src/opta.cpp`'s four per-direction sub-iterations
   (see F011 above). Pinned by `"a 2px-thick bar keeps a single connected
   skeleton"` (all 7 methods) and the general connectivity-preservation suite.
2. **Edge-pixel thinning via zero-pad** — `R/thin.R`'s pad-then-crop in
   `thin()` (see F012 above). Pinned by `"output has no foreground pixels on
   the matrix border"` and the exact per-method edge row-width pins.
3. **Distance-transform Inf sentinel on all-foreground** — `src/distance_transform.cpp`'s
   infinity seed for `manhattan`/`chessboard` (see F015 above). Pinned by
   `"distance_transform: all-foreground image returns Inf"` across all 3
   metrics.
4. **NA rejection in `as_binary_matrix`** — `R/thin.R`'s `anyNA()` guard (see
   F016 above). Pinned exhaustively across storage modes and re-verified to
   cover `distance_transform()`/`medial_axis()` too (they share the helper).

No regressions found in any of the four.

## Fresh findings (ranked by expected value)

### Finding 1 — F018 duplication fixed: `zhang_suen.cpp`/`lee.cpp`/`holt.cpp` now use the shared crossing-number helpers
- **Where:** `src/zhang_suen.cpp`, `src/lee.cpp`, `src/holt.cpp`.
- **What:** each had its own inline copy of the Zhang-Suen crossing-number
  (`A`) and neighbour-count (`B`) computation instead of `#include
  "thinr_common.h"` and calling `thinr::crossing_number()` /
  `thinr::neighbour_count()`, which `hilditch.cpp` already did. A future fix
  to the shared definition (exactly the kind of fix F014 just made) would
  silently not propagate to the three inlined copies.
- **Why it matters:** duplication risk, not a live bug — the three inline
  copies were token-identical to the header's definitions at the time of this
  review, so this was a latent risk (the next correctness fix to the shared
  definition silently missing three call sites), not an active discrepancy.
- **Fix:** added `#include "thinr_common.h"` to all three files and replaced
  each inline `int A = ...` / `int B = ...` with a call to
  `thinr::crossing_number(...)` / `thinr::neighbour_count(...)`. Purely
  mechanical — the replacement expressions are textually identical to what
  they replace, so this cannot change output. `guo_hall.cpp` and `k3m.cpp`
  were checked and correctly left alone (different algorithms, not
  duplicates).
- **Acceptance test:** full recompile + `devtools::test()` → `510/510 PASS`
  (unchanged from before the refactor — no test needed updating, itself
  confirming behaviour-preservation), `lintr::lint_package()` → `0`,
  `covr::package_coverage()` → unchanged at 100.00%.
- **FIX, not report** (mechanical, behaviour-preserving duplication removal,
  explicitly permitted under the auto-fix policy).

### Finding 2 — `CLAUDE.md` bookkeeping was stale (wrong version, wrong method count, missing the entire 0.3.0 fix wave) — FIXED
- **Where:** `CLAUDE.md`, "Current state" / "Testing conventions".
- **What:** three separately-wrong, mechanically-checkable claims:
  - `**Version:** 0.2.0` — `DESCRIPTION` says `0.3.0` (confirmed: `thinr`
    already shipped a 0.3.0 CRAN release carrying the whole 2026-07-03 review
    fix wave, per `NEWS.md`'s `# thinr 0.3.0` section).
  - `methods <- c(...)` described as `"(currently nine)"` — there are
    **seven** (`zhang_suen, guo_hall, lee, k3m, hilditch, opta, holt`,
    confirmed by counting `tests/testthat/test-thin.R`'s `methods` vector).
    This is a genuine internal self-contradiction: the very same `CLAUDE.md`
    documents, two paragraphs earlier, dropping `stentiford` and `pavlidis`
    (9 → 7) — the "(currently nine)" parenthetical was simply never updated
    after that drop.
  - "Recent shipments" had no entry at all for the 0.3.0 release (F011-F019),
    despite `NEWS.md` documenting it in full — the same doc-drift pattern the
    meta-repo's own `CLAUDE.md` names as the ecosystem's recurring disease.
  - "Acceptance: R CMD check --as-cran clean" — no longer literally true
    (see Finding 3): a local `--as-cran` run now reports 1 WARNING, for a
    reason that has nothing to do with package quality (see below).
- **Fix:** updated the version, corrected "nine" → "seven" with a dated
  `(as of 2026-07-28, from <source>)` tag, added a "Recent shipments" entry
  summarising the 0.3.0 fix wave (pointing to `NEWS.md` and this review file),
  and reworded the acceptance criterion to name the real gate
  (`R-CMD-check.yaml`, confirmed green) rather than a raw `--as-cran`
  invocation whose one WARNING is explained in Finding 3.
- **FIX, not report** (doc-only, mechanical).

### Finding 3 (informational, not fixed — no code or CI-config change needed) — two apparent CRAN/CI red flags investigated and explained
Two things surfaced during verification that looked concerning on the
surface; both were run to ground with live evidence rather than left as
ambiguous flags:

1. **`rcmdcheck(..., "--as-cran")` reports a WARNING**: *"Insufficient package
   version (submitted: 0.3.0, existing: 0.3.0)"*. This is CRAN's live
   incoming-feasibility check correctly refusing to accept a **resubmission
   of an already-published version** — it is positive confirmation that
   `thinr` 0.3.0 (carrying every fix in this review's recurrence table) is
   genuinely live on CRAN, not a defect. I confirmed the **real** CI gate
   does not hit this: `gh api "repos/humanpred/thinr/actions/workflows/R-CMD-check.yaml/runs?branch=main"`
   shows the run on **this exact reviewed SHA** (`2b08775a...`) completed
   `"conclusion":"success"`. **Recommendation (not mine to execute — a
   release-management decision, not a code-hygiene one):** bump `DESCRIPTION`
   to a dev version (`0.3.0.9000`) now that 0.3.0 is published, per standard
   R packaging convention, so a future contributor's local `--as-cran` check
   does not need this explanation re-derived. Left as **REPORT-ONLY**.
2. **`test-coverage.yaml` shows `"conclusion":"failure"` on this exact SHA**
   in `gh run list`. Traced to ground via `gh run view --log`: the test run
   itself inside that job printed `[ FAIL 0 | WARN 0 | SKIP 0 | PASS 510 ]` —
   fully clean — and the job only fails **afterward**, in the
   `codecov/codecov-action` step, which downloads the Codecov CLI and fails
   GPG-verifying its own signature: `gpg: Can't check signature: No public
   key` / `Could not verify signature. Please contact Codecov if problem
   continues.` This is Codecov's own infrastructure (a missing/rotated
   signing key in the action's fresh keyring), entirely unrelated to
   `thinr`'s code, tests, or coverage number, and `test-coverage.yaml` is a
   separate, non-blocking job from the real gate (`R-CMD-check.yaml`, which
   is green). **REPORT-ONLY** — not a code fix, and "Modifying CI
   configuration" requires explicit confirmation per this package's own
   `CLAUDE.md` workflow rules. If the README's Codecov badge staying stale is
   a problem, retrying the job or pinning a different `codecov-action`
   release/version would be the fix, but that is a CI-config change I did
   not make without asking.

### Finding 4 (considered, correctly ruled out) — are `medial_axis()`/`distance_transform()` the F083 dead-code shape?
Grepped the whole ecosystem: `thinr::medial_axis()` and
`thinr::distance_transform()` have **zero** non-test call sites anywhere in
`figureextractraster`/`figureextract`/`kmextract`/etc. — only `thinr::thin()`
is used in production (`figureextractraster::extract_raster()`, one call
site, no `max_iter` override). This is **not** the F083 dead-code pattern:
`thinr` is deliberately "the one public CRAN package" in the ecosystem (per
its own `CLAUDE.md`/`ARCHITECTURE.md`, ADR-007) — `medial_axis()` and
`distance_transform()` are core, independently-useful, 100%-covered public
API for `thinr`'s **external CRAN userbase**, not internal ecosystem glue
awaiting a caller. Unlike `figuretrace::assemble_fragments`/`pick_bridge`
(built for one specific internal consumer that never wired it up), these
functions are the reason the package is published at all. Correctly not
flagged as a finding.

## Silent-degradation hunt (item C) — summary

- **Defaults nobody overrides:** `thin(max_iter = 1000L)` is never overridden
  by the one ecosystem caller (`figureextractraster::extract_raster()`) —
  checked, and this is **not** a footgun default: it is a documented safety
  ceiling ("Real binary images of typical sizes converge well under 50
  passes; the limit is a safety bound against pathological inputs"), not a
  tuning knob whose wrong value silently degrades output the way
  `n_colours = 2L` did elsewhere in the ecosystem. No finding.
- **`tryCatch`/`try()` swallowing errors:** none in `R/` or `src/`.
- **NA / degenerate input:** rejected loudly and uniformly at the
  `as_binary_matrix()` boundary for every storage mode and every one of the
  three public entry points (F016, re-verified).
- **Empty vs. broken:** `thin()`/`medial_axis()`/`distance_transform()` on an
  all-background image correctly return all-zero/all-background output
  (tested); a malformed input (wrong dimensions, wrong storage mode, `NA`) is
  rejected with a classed, informative `stop()`, never silently coerced.
- **Magic numbers:** none found undocumented in R or C++; the K3M lookup
  tables and OPTA/Hilditch boolean expressions are transcriptions from named,
  cited papers (verified where checkable — see the EBImage section above for
  the standard of verification applied).

## Tests that enshrine bugs (item E)

**None found.** Every test whose name could sound like it pins a defect
(`"OPTA's N2 condition protects diagonal-2-neighbour patterns... leave stray
pixels at bar ends"`, the `opta`/`holt` `max_rows <- 3L` allowance in the
line-collapse test) is annotated in-line as **published, intentional
algorithm behaviour**, not an implementation bug, and is cross-checked
against the connectivity-preservation suite (which does hold OPTA to the
strict `count_components == 1` invariant on 2px bars). The isolated-2x2-block
divergence (F017) is a real, disclosed, tested difference between methods,
not a bug in either.

## Proposed gates (item F)

1. **(Done, this package)** F018's duplication fix itself is the gate — a
   future correctness change to `thinr::crossing_number()`/`neighbour_count()`
   now automatically reaches `zhang_suen`, `lee`, and `holt` instead of
   silently not reaching three of the seven kernels.
2. **(Proposed, meta-repo or this package's CI)** A repo-text tripwire for
   the false-equivalence-claim class of bug generally: `grep -rniE
   "drop-in replacement for [A-Za-z:]+::" .` (excluding `.git`) as a `make
   audit`-style check, so a *future* equivalence claim of this shape gets
   caught before merge rather than surviving to the next full ecosystem
   review. Not implemented here (would need to live in the meta-repo's
   `tools/audit.R`, out of this package's scope), but precise enough to add
   mechanically.
3. **(Proposed, not a code change)** Re-run or re-pin
   `codecov/codecov-action` in `.github/workflows/test-coverage.yaml` next
   time someone is in that file for an unrelated reason, to clear the
   GPG-verification failure (Finding 3.2). Did not do this myself since
   modifying CI configuration requires explicit confirmation per this
   package's own `CLAUDE.md`.
4. **(Proposed, not a code change)** Bump `DESCRIPTION`'s `Version` to a dev
   suffix (e.g. `0.3.0.9000`) now that `0.3.0` is published to CRAN, per
   standard practice, so `--as-cran` checks stop reporting the (harmless but
   confusing) "already exists" WARNING locally (Finding 3.1). A
   release-management decision, not mine to make unilaterally.

## What I fixed vs. reported

**Fixed (mechanical, behaviour-preserving, on branch `review/2026-07-28`,
2 commits):**
- F018: `zhang_suen.cpp`/`lee.cpp`/`holt.cpp` now use `thinr::crossing_number()`/
  `thinr::neighbour_count()` from `thinr_common.h` instead of duplicating the
  inline expression.
- Corrected `CLAUDE.md`'s stale version, method count, acceptance-criteria
  wording, and missing 0.3.0 shipment history.

**Reported only (release-management or third-party-infrastructure, not code
hygiene, not appropriate for this branch):**
- Bumping `DESCRIPTION` to a dev version post-CRAN-publication (Finding 3.1).
- The Codecov GPG-verification failure in `test-coverage.yaml` (Finding 3.2)
  — third-party infra, and CI-config changes need explicit confirmation.
- The general false-equivalence-claim `audit.R` gate proposal (item 2 above)
  — belongs in the meta-repo, out of this package's scope.

All fixes verified together with a final `devtools::test(stop_on_failure =
TRUE)` → `510/510 PASS, 0 FAIL, 0 WARN`, `lintr::lint_package()` → `0`, and
`covr::package_coverage()` → **100.00%** (all files, R and C++). The real CI
gate (`R-CMD-check.yaml`) was independently confirmed green on the exact
reviewed SHA via `gh api`, not assumed from the README badge.
